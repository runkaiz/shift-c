name = "shift-c-cron-checkin"
main = "src/index.js"
compatibility_date = "2024-09-23"

[triggers]
crons = ["*/15 * * * *"]

[vars]
# The Pages app's send-checkins endpoint. Override in production once the
# real domain is live (defaults here target local dev).
TARGET_URL = "http://localhost:8788/api/cron/send-checkins"
